<p><%= link_to("Back to all entries", entries_path, class: "btn btn-default") %></p>

<h2><%= @entry.title %></h2>
<p><%= @entry.date %></p>
<p><%= @entry.contents %></p>

<p><%= link_to("Edit this entry", edit_entry_path(@entry), class: "btn btn-default") %></p>
<p><%= link_to("Delete", entry_path(@entry), method: :delete, "class" => "btn btn-danger", "data-confirm" => "Are you sure?") %></p>